1. **Create Migration:** Use `run_in_bash_session` to create a new migration file `migrations/030_trivia.sql` with a `trivia_scores` table (`id SERIAL PRIMARY KEY`, `guild_id VARCHAR(255) NOT NULL`, `user_id VARCHAR(255) NOT NULL`, `score INT DEFAULT 0`, `UNIQUE(guild_id, user_id)`). Then verify using `read_file`.
2. **DB Operations:** Use `replace_with_git_merge_diff` on `internal/db/db.go` to add `TriviaScore` struct, `AddTriviaScore` (upserts score and increments by 1), `GetTriviaLeaderboard` (returns top 10 users by score in a guild), and `AddCoins` (adds a specific amount of coins to a user). Verify using `go build ./...`.
3. **Trivia Command:** Use `write_file` to create `internal/bot/commands/trivia.go` with `NewTriviaCommand`. The `/trivia start` command will fetch a question from `https://opentdb.com/api.php?amount=1` (with `html.UnescapeString`), store the correct answer in memory or use a smart `CustomID` (like `trivia_{correct_index}_{question_id}`), and send a message with buttons. `/trivia leaderboard` will use `GetTriviaLeaderboard`. Verify using `cat`.
4. **Interaction Handler:** Use `replace_with_git_merge_diff` on `internal/bot/bot.go`'s `interactionCreateHandler`. Add logic to intercept `discordgo.InteractionMessageComponent` with a `CustomID` starting with `trivia_`. It will parse the ID, check if the answer is correct. If correct, update the user's score via `AddTriviaScore`, award 10 coins via `AddCoins`, edit the message to show the correct answer, disable buttons, and announce the winner. If wrong, edit to show wrong answer and disable. Verify using `cat` and `go build ./...`.
5. **Register Command:** Use `replace_with_git_merge_diff` on `internal/bot/bot.go` inside the `New()` function to add `registry.Add(commands.NewTriviaCommand(database))` or similar, so the command is registered. Verify using `go build ./...`.
6. **Testing:** Run `go test ./...` and `timeout 10 go run ./cmd/bot/ > bot.log 2>&1 &` then `cat bot.log` to verify the bot starts correctly and commands register.
7. **Pre-commit Checks:** Complete pre-commit steps to ensure proper testing, verification, review, and reflection are done.
8. **Update AGENTS.md:** Check off Phase 33 items and add a "Completed Work" section using `replace_with_git_merge_diff`.
9. **Submit:** Open a PR.
